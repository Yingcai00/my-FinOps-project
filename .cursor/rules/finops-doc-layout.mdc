---
description: FinOps 文档目录、html 原型、superpowers 产出路径、reference 及手绘流程为准则
alwaysApply: true
---

# FinOps 文档与资产目录约定

## 产出与存放位置

| 类型 | 目录 | 说明 |
|------|------|------|
| AI 生成的流程图、流程说明（含研发团队规范类流程文本） | `AI流程图/` | 新建或更新流程图类产出时写入此目录 |
| AI 生成的需求、设计说明类 Markdown | `AI需求文档/` | 需求文档、方案与需求合并稿等写入此目录 |
| 待读取的数据文件、模型说明、读取与分析脚本 | `read_data/` | 数据、模型定义及 Python 等分析脚本统一放此目录 |
| 原型设计用的输入提示词 | `原型提示文档/` | 提供给 AI/工具的「原型设计」提示词、提示文档放此目录 |
| 生成的系统原型页面（HTML 等） | `html/` | AI 或工具产出的可浏览原型、演示页写入此目录 |

## Superpowers 产出（目录例外）

由 **superpowers** 工作流生成的文件（例如 `docs/superpowers/` 下的 `specs`、以及 superpowers 约定的其它子目录），**保持 superpowers 原有的目录结构即可**，放在对应路径下维护。

- **不要**为「与 `AI需求文档/` 等目录统一」而迁移或合并这些文件。
- 文内引用、注释中的路径也以 **superpowers 树内路径** 为准（如 `docs/superpowers/specs/...`）。

与上表「AI 需求文档」的关系：`AI需求文档/` 用于本项目常规 AI 产出需求说明；**superpowers 规格/计划类产物不在此列**，二者并存、互不搬迁。

## reference（只作参考）

- 目录：`reference/`
- 用途：竞品设计规则、页面原型、交互参考；**不当作本项目的唯一事实来源**。
- 设计或实现时：可对照竞品，但最终以项目内需求与流程为准。

## 手绘流程图（确定性业务流程，优先级最高）

- 目录：`手绘流程图/`
- 用途：用户提供的**确定性**业务流程（常以手绘/文档形式整理后放在此目录）。
- **准则**：涉及业务流程时，**以 `手绘流程图/` 中的内容为准**。
- **强制询问**：若你认为需要对手绘流程做**优化、扩展或偏离原文**，**必须先向用户确认**，得到明确同意后再改；不要默认替用户「优化」流程。

## 协作习惯

- 新建文件时优先落在上表对应目录，避免散落在仓库根目录；**superpowers 生成的文件除外**，按上文「Superpowers 产出」一节处理。
- 若目录不存在再创建同名目录（与上表中文名一致）。
