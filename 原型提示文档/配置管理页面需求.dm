# FinOps 系统 - 配置管理模块需求文档

## 模块概述
**模块名称**：配置管理模块  
**页面名称**：配置管理页面  
**核心用户**：系统管理员、IT运营管理员  
**设计目标**：提供灵活、可扩展的配置管理界面，允许管理员自定义和维护系统各类下拉选项值，确保成本管理的适应性和可扩展性

## 页面一：配置管理主页

### 页面目标
集中管理系统所有可配置的下拉选项值，提供分类管理和批量操作能力

### URL
`/config/management`

### 布局与模块

#### 1. 顶部导航与筛选区
- **配置分类导航**：水平Tab导航，显示所有配置分类
  ```
  配置分类：成本池 | 成本子池 | 地域 | 公摊维度 | 公摊类型 | 单位 | 瓶颈资源类型 | 其他配置
  ```
- **快速搜索**：在选定分类下，支持按配置值名称或编码搜索
- **状态筛选**：支持按"启用/禁用"状态筛选配置项

#### 2. 配置操作区
- **主要操作按钮**：
  - `新增配置项`：在当前分类下添加新配置值
  - `批量导入`：支持从Excel批量导入配置项
  - `批量启用/禁用`：批量修改配置项状态
  - `导出配置`：导出当前分类下的所有配置项

#### 3. 配置列表区
- **表格列设计**：
  | 字段 | 说明 | 是否可编辑 |
  |------|------|------------|
  | 选择框 | 支持多选 | - |
  | 配置名称 | 配置项显示名称 | 是（双击编辑） |
  | 配置编码 | 系统内部唯一标识码 | 是（双击编辑，新建后可改） |
  | 排序号 | 决定下拉框中显示顺序 | 是 |
  | 状态 | 启用/禁用 | 是（开关控件） |
  | 关联配置 | 关联到其他配置（如成本子池关联成本池） | 是（下拉选择） |
  | 描述 | 配置项详细说明 | 是 |
  | 创建时间 | 系统记录 | 否 |
  | 更新时间 | 系统记录 | 否 |
  | 操作 | 编辑、删除 | - |

### 主要交互
1. **切换分类**：点击分类Tab，列表自动刷新显示该分类下的配置项
2. **快速编辑**：双击表格中的可编辑单元格，直接进行编辑，支持Enter保存、ESC取消
3. **批量操作**：勾选多个配置项后，上方操作按钮激活，可批量执行操作
4. **删除保护**：删除配置项时，系统检查是否被引用，如被引用则提示"有X条数据引用此配置，无法删除"

## 页面二：配置项详情/编辑页面

### 页面目标
查看或编辑单个配置项的详细信息，管理关联关系和高级属性

### URL
`/config/detail/:category/:id`

### 页面布局
- **顶部**：返回按钮 + 面包屑导航 + 页面标题
- **中部**：Tab切换组件

### Tab 1: 基础信息
- **表单字段**：
  ```
  配置标识
  ├─ 配置名称：文本输入，必填，最大长度50
  ├─ 配置编码：文本输入，必填，字母数字下划线，唯一性校验
  └─ 所属分类：下拉选择，从配置分类中选择，只读（从URL传入）
  
  显示设置
  ├─ 排序号：数字输入，决定下拉框显示顺序
  └─ 显示颜色：颜色选择器（可选），用于在图表中区分
  
  状态管理
  ├─ 启用状态：开关控件
  └─ 生效时间：日期范围选择（可选），可设置配置项生效时段
  
  描述信息
  └─ 详细描述：多行文本，用于说明此配置项的用途和使用场景
  ```

### Tab 2: 关联配置（根据分类动态显示）
- **关联关系配置**：
  - **成本子池**：需要关联到"成本池"
    ```
    关联成本池：下拉选择，可选值来自"成本池"分类
    ```
  - **地域**：可设置层级关系
    ```
    上级地域：下拉选择，可选值来自"地域"分类，可为空（表示顶级）
    地域代码：文本输入，如"CN-BJ"
    ```
  - **单位**：可设置单位换算关系
    ```
    基准单位：单选，标记此单位是否为基准单位
    换算系数：数字输入，相对于基准单位的换算系数
    单位类型：下拉选择（数量单位、容量单位、时间单位等）
    ```

### Tab 3: 扩展属性
- **动态属性管理**：
  ```
  功能：允许管理员为特定分类的配置项添加自定义属性
  
  示例：
  为"成本池"分类添加扩展属性：
  ├─ 负责人：文本输入
  ├─ 预算上限：数字输入
  └─ 所属部门：下拉选择
  
  为"瓶颈资源类型"添加扩展属性：
  ├─ 计量单位：下拉选择
  ├─ 默认单价：数字输入
  └─ 资源图标：图标选择器
  ```
- **交互**：
  - `添加扩展属性`按钮：弹出表单，定义新属性
  - 属性列表：显示已定义的扩展属性及其值

### Tab 4: 引用检查
- **显示当前配置项被引用的位置**：
  ```
  引用统计
  ├─ 成本项引用：X 条
  ├─ 产品定义引用：X 条
  └─ 定价规则引用：X 条
  
  引用详情列表
  ├─ 引用类型
  ├─ 引用对象
  ├─ 引用字段
  └─ 操作（查看详情）
  ```

## 页面三：配置分类管理页面

### 页面目标
管理系统配置分类，定义分类属性和行为

### URL
`/config/categories`

### 页面布局

#### 1. 分类列表区
- **表格列**：
  | 字段 | 说明 |
  |------|------|
  | 分类名称 | 配置分类的显示名称 |
  | 分类编码 | 系统内部标识，用于API调用 |
  | 是否系统内置 | 标记是否为系统内置分类（不可删除） |
  | 配置项数量 | 该分类下的配置项总数 |
  | 关联模型 | 关联的数据模型（如成本项、产品等） |
  | 操作 | 编辑、禁用（非系统内置分类可删除） |

#### 2. 分类详情/编辑弹窗
- **字段分组**：
  ```
  基础信息
  ├─ 分类名称：文本输入
  ├─ 分类编码：文本输入（新建时），只读（编辑时）
  └─ 分类描述：多行文本
  
  行为配置
  ├─ 是否允许层级结构：开关（如地域需要层级）
  ├─ 是否允许关联其他配置：开关（如成本子池关联成本池）
  ├─ 是否启用扩展属性：开关
  └─ 是否允许用户自定义值：开关（在下拉框中是否允许用户输入新值）
  
  显示配置
  ├─ 默认排序字段：下拉选择（按编码、按名称、按排序号）
  ├─ 列表页面显示字段：多选（编码、名称、描述、状态等）
  └─ 表单中显示为：单选（下拉框、单选组、多选框、级联选择）
  ```

## 页面四：批量导入配置页面

### 页面目标
通过Excel模板批量导入配置项，支持数据校验和冲突处理

### 访问方式
从配置管理主页点击`批量导入`按钮进入

### 页面布局

#### 1. 导入向导（分步流程）
```
步骤1：选择配置分类
├─ 选择要导入的配置分类
└─ 下载对应分类的Excel模板

步骤2：上传数据文件
├─ 拖拽上传或点击上传
├─ 支持格式：.xlsx, .xls
└─ 文件大小限制：10MB

步骤3：数据映射与校验
├─ 系统自动映射Excel列与系统字段
├─ 数据预览（显示前10行）
├─ 校验结果：成功X条，警告Y条，错误Z条
└─ 可点击错误项查看详细错误信息

步骤4：冲突处理策略
├─ 发现重复编码时的处理方式：
│   ├─ 跳过（保留现有数据）
│   ├─ 覆盖（用导入数据替换）
│   └─ 创建新记录（修改编码后新建）
├─ 关联配置不存在时的处理：
│   ├─ 跳过该条记录
│   └─ 创建关联配置（需指定默认值）
└─ 批量操作设置：导入后是否自动启用所有配置项

步骤5：确认导入
├─ 显示最终导入统计
├─ 生成导入报告（可下载）
└─ 完成导入，返回配置管理页面
```

## 页面五：配置历史与审计页面

### 页面目标
记录配置项的变更历史，提供审计追踪功能

### URL
`/config/history`

### 页面布局

#### 1. 筛选条件区
- **筛选字段**：
  - 变更时间范围
  - 变更类型（新增、修改、删除、启用、禁用）
  - 配置分类
  - 操作人
  - 配置项名称/编码

#### 2. 变更历史列表
- **表格列**：
  | 字段 | 说明 |
  |------|------|
  | 变更时间 | 精确到秒 |
  | 配置分类 | 所属配置分类 |
  | 配置项 | 配置项名称和编码 |
  | 变更类型 | 新增/修改/删除等 |
  | 变更摘要 | 简要说明变更内容 |
  | 操作人 | 执行变更的用户 |
  | IP地址 | 操作来源IP |
  | 操作 | 查看详情 |

#### 3. 变更详情弹窗
- **显示完整变更信息**：
  ```
  变更概要
  ├─ 变更时间：2024-01-15 14:30:25
  ├─ 操作人：张三 (zhangsan)
  ├─ 操作类型：修改
  └─ 操作终端：Web管理后台 (IP: 192.168.1.100)
  
  变更内容对比
  ├─ 采用左右对比视图显示字段变更
  ├─ 左侧：变更前的值
  ├─ 右侧：变更后的值
  └─ 变更字段高亮显示
  ```

## 系统配置项分类定义

### 1. 核心配置分类
| 分类名称 | 编码 | 是否系统内置 | 是否允许层级 | 是否允许关联 |
|----------|------|--------------|--------------|--------------|
| 成本池 | cost_pool | 是 | 否 | 否 |
| 成本子池 | cost_sub_pool | 是 | 否 | 是（关联成本池） |
| 地域 | region | 是 | 是 | 是（关联上级地域） |
| 公摊维度 | share_dimension | 是 | 否 | 否 |
| 公摊类型 | share_type | 是 | 否 | 否 |
| 单位 | unit | 是 | 否 | 否 |
| 瓶颈资源类型 | bottleneck_resource | 是 | 否 | 否 |

### 2. 扩展配置分类（示例）
| 分类名称 | 编码 | 是否系统内置 | 用途 |
|----------|------|--------------|------|
| 数据来源 | data_source | 是 | 成本项的数据来源 |
| 资产分类 | asset_category | 是 | 资产系统分类映射 |
| 折旧方法 | depreciation_method | 是 | 资产折旧计算方法 |
| 成本状态 | cost_status | 是 | 成本项的生命周期状态 |

## 权限与安全设计

### 1. 权限控制
- **配置查看权限**：所有用户可查看启用的配置项
- **配置管理权限**：仅"系统管理员"和"配置管理员"角色可增删改配置
- **系统内置配置**：仅超级管理员可修改

### 2. 数据验证规则
- **唯一性验证**：配置编码在分类内必须唯一
- **引用保护**：被引用的配置项不能被删除，只能禁用
- **编码格式**：只允许字母、数字、下划线，不能以数字开头
- **名称长度**：配置名称不超过50字符

### 3. 批量操作限制
- **单次导入限制**：不超过1000条记录
- **批量启用/禁用**：每次最多操作100条
- **操作频率限制**：同一用户10分钟内最多执行5次批量操作

## API接口设计要点

### 1. 配置项管理API
```
GET    /api/config/{category}          # 获取分类下的配置项列表
GET    /api/config/{category}/{code}   # 获取单个配置项详情
POST   /api/config/{category}          # 新增配置项
PUT    /api/config/{category}/{code}   # 更新配置项
DELETE /api/config/{category}/{code}   # 删除配置项（软删除）
PATCH  /api/config/{category}/batch    # 批量更新配置项
```

### 2. 配置下拉框数据API
```
GET /api/config/options/{category}     # 获取分类的下拉选项（仅启用的）
参数：include_disabled=true    # 包含禁用的选项
参数：tree=true                # 返回树形结构（用于层级配置）
```

### 3. 导入导出API
```
POST   /api/config/import/preview     # 预览导入数据
POST   /api/config/import/execute     # 执行导入
GET    /api/config/export/{category}  # 导出配置项
```

## 交互细节设计

### 1. 实时搜索与过滤
- 在配置管理页面，输入搜索内容时实时过滤列表
- 支持拼音首字母搜索（如输入"bj"可搜索到"北京"）

### 2. 拖拽排序
- 在配置列表中，支持通过拖拽调整配置项的排序号
- 拖拽时显示视觉反馈和排序预览

### 3. 配置项依赖检查
- 修改配置项编码时，检查是否有其他配置项依赖此编码
- 禁用配置项时，提示可能影响的功能模块

### 4. 配置缓存管理
- 提供"清除配置缓存"按钮，强制刷新前端缓存
- 配置变更后，相关用户界面自动刷新

### 5. 配置快照与恢复
- 支持创建配置快照，记录特定时间点的配置状态
- 可从快照恢复配置，用于版本回滚

## 响应式设计

### 1. 桌面端优化
- 配置列表支持固定列和横向滚动
- 多列布局充分利用屏幕空间

### 2. 移动端适配
- 配置管理页面以卡片列表形式展示
- 编辑页面采用全屏弹窗或独立页面

### 3. 打印优化
- 配置列表和详情页面支持打印友好格式
- 打印时自动隐藏操作按钮和无关元素

---

**文档版本**：v1.0  
**更新日期**：2026-02-01  
**文档状态**：✓ 需求设计完成  
**关联模块**：成本管理、产品管理、计费管理