# FinOps 系统 - 成本管理模块需求文档

## 系统概述
**系统名称**：FinOps财务运营管理平台  
**模块名称**：成本管理模块  
**核心用户**：IT运维人员、财务运营人员  
**设计目标**：清晰展示成本结构，高效管理成本数据，完成成本分摊，支撑产品定价

## 页面一：成本管理列表页

### 页面目标
总览成本，进行批量操作和日常数据维护

### URL
`/cost/management`

### 布局与模块

#### 1. 顶部全局概览区 (Section A)
- 并排三个数据卡片：
  - **本月总成本**：`[数值]元`，环比`[+/-X%]`
  - **月末预测成本**：`[数值]元`
  - **待分摊公摊成本**：`[数值]元`（醒目颜色，如橙色）

#### 2. 查询与筛选区 (Section B)
- 一行表单，包含以下筛选条件：
  - 下拉框：成本池、地域、公摊类型、数据来源
  - 输入框：成本项名称、账期
  - 按钮：`查询`、`重置`、`高级筛选`（展开更多条件）
- **交互**：筛选条件变化后，列表与顶部概览数据联动刷新

#### 3. 操作按钮区 (Section C)
- 按钮水平排列：
  - `批量导入`
  - `自动同步`
  - `手动添加`
  - `执行公摊计算`（主按钮，蓝色）
- `执行公摊计算`按钮旁可添加状态徽章，如"待执行"

#### 4. 数据表格区 (Section D)
- 多选复选框表头
- **固定列**：`成本项`、`公摊类型`、`月成本`、`公摊后月总成本`、`操作`
- **滚动列**：
  | 字段 | 说明 |
  |------|------|
  | 成本编码 | 系统生成唯一标识 |
  | 成本池 | 基础资源成本、网络成本等 |
  | 成本子池 | 设备费用、软件平台等 |
  | 地域 | 北京、上海、西安等 |
  | 公摊维度 | 根据业务定义 |
  | 描述 | 成本项详细说明 |
  | 单价（元） | 只能输入数字，两位小数 |
  | 折旧月限 | 整数 |
  | 折旧月价 | 自动计算：单价/折旧月限 |
  | 数量 | 整数 |
  | 单位 | 台、度、平、人等 |
  | 账期 | 自动填写，灰显不可编辑 |
  | CPU总量 | 整数，单位：核 |
  | 内存总量 | 整数，单位：GB |
  | 存储总量 | 整数，单位：TB |
  | 流量总量 | 整数，单位：GB |
  | 数据来源 | 资产系统-B类资产、手动维护 |
  | 创建时间 | 系统自动记录 |
  | 更新时间 | 系统自动记录 |

- **交互特性**：
  - 支持按任意列排序
  - `公摊后月总成本`列：未执行分摊计算前显示"--"，计算后显示数值并高亮
  - 点击成本项名称或`详情`按钮 → 跳转至`成本详情页`
  - 点击`编辑`按钮 → 跳转至`成本详情页`的编辑状态
  - 点击`删除`按钮 → 弹出二次确认框

### 主要交互流程

#### 1. 批量导入流程
```
点击`批量导入` → 弹出模态框
├─ 步骤1：选择数据来源
│   ├─ 资产系统-B类资产
│   └─ 行政-电费
├─ 步骤2：下载对应模板
│   └─ Excel模板字段与手动添加表单一致（排除账期）
├─ 步骤3：上传填写好的文件（支持拖拽）
├─ 步骤4：数据预览与校验
├─ 步骤5：选择导入选项
│   ├─ 替换现有数据
│   └─ 追加数据
└─ 步骤6：确认导入 → 显示导入结果报告
```

#### 2. 自动同步流程
```
点击`自动同步` → 显示同步状态
├─ 同步中：显示进度条/加载动画
├─ 同步成功：显示"已更新X条记录"
└─ 同步失败：显示错误详情和重试选项
```

#### 3. 执行公摊计算流程
```
点击`执行公摊计算` → 弹出确认框
├─ 确认执行 → 系统处理
├─ 处理完成 → 弹出结果报告
│   ├─ 总公摊成本额
│   ├─ 分摊影响条数
│   └─ 计算状态（成功/失败）
└─ 关闭报告 → 表格数据刷新
```

## 页面二：成本详情页

### 页面目标
查看或编辑单一成本项的完整信息，管理其关联的瓶颈资源

### URL
`/cost/detail/:id`

### 页面布局
- 顶部：返回按钮 + 标题"成本详情：[成本项名称]"
- 中部：Tab切换组件

### Tab 1: 成本详情

#### 查看态
- **信息分组展示**：
  ```
  基础信息组
  ├─ 成本池：下拉选择，必填
  ├─ 成本子池：下拉选择，必填（手动维护时显示）
  ├─ 成本项：单行文本，必填
  ├─ 地域：下拉选择，必填
  ├─ 公摊维度：下拉选择，必填
  └─ 公摊类型：下拉选择，必填
  
  财务信息组
  ├─ 单价（元）：数字输入，两位小数，必填
  ├─ 折旧月限：整数输入，必填
  ├─ 折旧月价：自动计算显示，灰显
  ├─ 数量：整数输入，必填
  ├─ 单位：下拉选择，必填
  └─ 月成本（元）：自动计算显示，灰显
  
  公摊信息组
  ├─ 公摊比例：自动计算，灰显
  └─ 公摊后月总成本：自动计算，灰显
  
  扩展信息组
  ├─ CPU总量：整数输入
  ├─ 内存总量：整数输入
  ├─ 存储总量：整数输入
  └─ 流量总量：整数输入
  
  系统信息组
  ├─ 成本编码：系统生成，灰显
  ├─ 数据来源：系统记录，灰显
  ├─ 创建人：系统记录，灰显
  ├─ 创建时间：系统记录，灰显
  ├─ 更新人：系统记录，灰显
  └─ 更新时间：系统记录，灰显
  ```
- **功能按钮**：右侧显示`编辑`按钮

#### 编辑态
- 所有可编辑字段变为表单控件
- **实时计算**：修改`单价`、`数量`、`折旧月限`时，`月成本`实时更新预览
- **底部按钮**：`保存`、`取消`

### Tab 2: 瓶颈资源

#### 功能按钮
- `添加瓶颈资源`按钮（顶部右侧）

#### 数据表格
| 字段 | 说明 |
|------|------|
| 瓶颈资源 | 资源名称（如CPU、内存、磁盘） |
| 资源总量 | 数字，单位根据资源类型确定 |
| 单价计算公式 | 只读，如`公摊后月总成本/资源总量` |
| 单价 | 自动计算显示，只读 |
| 操作 | 删除按钮 |

#### 交互流程
```
点击`添加` → 弹出表单
├─ 字段：
│   ├─ 瓶颈资源名称（下拉/输入）
│   ├─ 资源总量（数字输入）
│   └─ 单位（下拉选择）
├─ 点击`保存` → 表格新增一行
└─ 系统自动计算`单价` = `公摊后月总成本` / `资源总量`
```

## 页面三：成本分析页

### 页面目标
多维度、可视化分析成本数据，生成洞察报告

### URL
`/cost/analysis`

### 布局与模块

#### 1. 分析控制区
- **筛选条件**：
  - 分类维度：下拉框（成本池、成本子池、成本项、地域）
  - 金额类型：下拉框（公摊后月总成本、月成本）
  - 时间粒度：单选按钮（年、月）
  - 时间范围：日期选择器
- **功能按钮**：`更新分析`（主按钮）

#### 2. 图表可视化区
- **动态图表显示**：
  ```
  默认状态：显示"近12个月总成本趋势（折线图）"
  
  根据筛选条件自动推荐：
  ├─ 时间粒度="月"，分类维度="成本池" → 堆叠柱状图
  ├─ 时间粒度="月"，分类维度="无" → 折线图
  └─ 时间粒度="年"，分类维度="成本项" → 分组柱状图
  ```
- **图表交互**：
  - 悬停：显示数据点详情
  - 点击：支持下钻（如点击柱状图中"服务器"部分，显示该分类下所有成本项）

#### 3. 数据明细区
- 标题：右侧有`导出Excel`按钮
- 表格：显示聚合后的明细数据
- 列：根据`分类维度`动态变化

#### 4. 报告功能区
- 按钮：`保存为报告`（右上角）
- 交互：
  ```
  点击`保存为报告` → 弹出对话框
  ├─ 输入：报告名称、描述
  ├─ 点击`保存` → 将当前分析视图存入"我的报告"库
  └─ 系统提示"保存成功"
  ```

## 全局设计与交互规范

### 1. 设计系统
- **风格**：现代、简洁的B端设计风格
- **组件库**：Ant Design / Element UI
- **色彩规范**：
  - 主色：蓝色（#1890FF）
  - 成功色：绿色（#52C41A）
  - 警告色：橙色（#FA8C16）
  - 错误色：红色（#F5222D）

### 2. 数据可视化
- **图表色板**：清晰、区分度高
- **图表类型**：折线图、柱状图、堆叠柱状图
- **响应式**：图表自适应容器宽度

### 3. 状态反馈
- **操作反馈**：
  - 保存成功：右上角Toast提示"保存成功"
  - 删除确认：二次确认对话框
  - 导入结果：详细的结果报告弹窗
- **加载状态**：
  - 数据加载：显示骨架屏或加载动画
  - 计算中：禁用相关按钮，显示进度提示

### 4. 响应式设计
- **主要适配**：桌面端（≥1280px宽度）
- **表格优化**：横向滚动时，操作列冻结
- **图表响应**：根据容器宽度自动调整图表尺寸

## 数据验证规则

### 1. 表单验证
- **必填字段**：带有红色星号(*)标注
- **数字字段**：只能输入数字，支持小数位数限制
- **下拉选择**：必须从选项中选择，不能为空

### 2. 业务规则验证
- **成本关联校验**：同一成本项被多个产品关联时，权重总和必须等于100%
- **公摊计算校验**：所有公摊成本必须被100%分摊
- **数据一致性**：导入/同步时检查数据格式和关联关系

## 权限管理

### 1. 角色权限
| 角色 | 成本管理权限 |
|------|--------------|
| IT运维 | 全部操作权限（增删改查、导入、公摊计算） |
| 财务运营 | 查看、分析、导出权限 |
| 业务部门 | 仅查看权限（成本分析页） |

### 2. 操作日志
- 系统自动记录关键操作：
  - 数据新增/修改/删除
  - 批量导入操作
  - 公摊计算执行
  - 报告保存

---

**文档版本**：v1.1  
**更新日期**：2026-02-01  
**文档状态**：✓ 已完成需求梳理  
**下一步**：UI设计、开发排期、测试用例编写